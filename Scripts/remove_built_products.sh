# Removes the built products in the built products directory.

rm -fr "$BUILT_PRODUCTS_DIR/SylvesterXPCService.xpc"
rm -fr "$BUILT_PRODUCTS_DIR/SylvesterXPC.framework"
rm -fr "$BUILT_PRODUCTS_DIR/SylvesterCommon.framework"
